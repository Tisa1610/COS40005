# Core API (collector on :8000)
fastapi==0.111.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
psutil==6.0.0
PyYAML==6.0.2
requests==2.32.3

# File watching (agent)
watchdog==4.0.1

# Auth service (on :8010)
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1

# Optional: load .env files if you want
python-dotenv==1.0.1
